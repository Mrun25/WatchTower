# Sample Project

A tiny demo app with a JS frontend and a Python (Flask) backend, used to
exercise Watchtower's cross-language relationship mapping.

- `frontend/userProfile.js` fetches and updates user profiles via the API.
- `backend/users.py` implements the `/api/users/<id>` GET and PUT routes.
